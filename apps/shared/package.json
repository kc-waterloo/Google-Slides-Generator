{
  "name": "@gsg/shared",
  "version": "0.0.1",
  "description": "Shared types, schemas, and validation for Google Slides Generator",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "@types/jest": "^30.0.0"
  }
}
